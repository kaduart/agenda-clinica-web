rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /appointments/{appointment} {
      allow read, write: if request.auth != null;
    }
    match /professionals/{professional} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}